'use client';
import {useState} from 'react'; import {createClient} from '@/lib/supabase-browser'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e){e.preventDefault();setBusy(true);setError('');const {error}=await createClient().auth.signInWithPassword({email:email.trim(),password});if(error){setError(error.message);setBusy(false);return}window.location.href='/account'}return <main className="min-h-screen grid place-items-center bg-[#fff7fa] p-5"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-xl"><Link href="/" className="text-sm font-semibold text-[#d9295f]">← PinkBox</Link><h1 className="mt-8 text-3xl font-bold">Welcome back</h1><p className="mt-2 text-gray-500">Sign in to view your account and orders.</p><input className="mt-8 w-full rounded-xl border p-3" required autoComplete="email" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="mt-3 w-full rounded-xl border p-3" required autoComplete="current-password" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="mt-5 w-full rounded-xl bg-[#d9295f] p-3 font-semibold text-white" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><p className="mt-5 text-sm text-gray-600">New customer? <Link className="font-semibold text-[#d9295f]" href="/account/signup">Create account</Link></p></form></main>}
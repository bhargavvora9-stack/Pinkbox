import {createServerClient} from '@supabase/ssr'; import {cookies} from 'next/headers';
export const dynamic='force-dynamic'; export const revalidate=0;
async function client(){const jar=await cookies();return createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,{cookies:{getAll(){return jar.getAll()},setAll(){}}})}
export async function GET(){try{const db=await client();const {data:{user},error:a}=await db.auth.getUser();if(a||!user)return Response.json({error:'Unauthorized'},{status:401});const {data:settings}=await db.from('website_settings').select('company_id').eq('slug','pinkbox').eq('status','active').maybeSingle();if(!settings)return Response.json({error:'Store unavailable'},{status:404});const {data:orders,error}=await db.from('website_orders').select('id,order_number,total_amount,order_status,payment_status,created_at,shipping_address').eq('company_id',settings.company_id).eq('customer_email',user.email).order('created_at',{ascending:false}).limit(50);if(error)return Response.json({error:error.message},{status:500});return Response.json({orders:orders||[]},{headers:{'Cache-Control':'private, no-store'}})}catch(e){return Response.json({error:'Unable to load orders.'},{status:500})}}

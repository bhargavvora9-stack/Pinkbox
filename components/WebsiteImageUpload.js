'use client';
import {useRef,useState} from 'react';
import {ImagePlus,Loader2,X} from 'lucide-react';

export default function WebsiteImageUpload({value,onChange,label='Image',folder='general'}){
 const ref=useRef(null); const [busy,setBusy]=useState(false); const [error,setError]=useState('');
 const upload=async file=>{if(!file)return;setError('');if(!file.type.startsWith('image/')){setError('Please select an image file.');return}if(file.size>5*1024*1024){setError('Image must be 5 MB or smaller.');return}setBusy(true);try{const fd=new FormData();fd.append('file',file);fd.append('folder',folder);const r=await fetch('/api/website/upload-image',{method:'POST',body:fd});const j=await r.json();if(!r.ok)throw Error(j.error||'Upload failed.');onChange(j.url)}catch(e){setError(e.message)}finally{setBusy(false)}};
 return <div className="space-y-2"><span className="block text-sm font-semibold text-gray-800">{label}</span><div className="flex items-center gap-3 rounded-xl border border-dashed border-gray-300 bg-gray-50 p-3"><button type="button" disabled={busy} onClick={()=>ref.current?.click()} className="inline-flex shrink-0 items-center gap-2 rounded-lg bg-gray-900 px-3 py-2 text-sm font-semibold text-white disabled:opacity-50">{busy?<Loader2 size={16} className="animate-spin"/>:<ImagePlus size={16}/>} {busy?'Uploading…':'Choose Image'}</button><input ref={ref} type="file" accept="image/jpeg,image/png,image/webp,image/gif,image/svg+xml" className="hidden" onChange={e=>{upload(e.target.files?.[0]);e.target.value=''}}/><span className="truncate text-xs text-gray-500">JPG, PNG, WEBP, GIF or SVG · max 5 MB</span></div>{value&&<div className="relative overflow-hidden rounded-xl border bg-white p-2"><img src={value} alt="Preview" className="h-32 w-full object-contain"/><button type="button" onClick={()=>onChange('')} className="absolute right-2 top-2 rounded-full bg-white/90 p-1.5 text-gray-700 shadow" aria-label="Remove image"><X size={15}/></button></div>}{error&&<p className="text-xs text-red-600">{error}</p>}</div>}
